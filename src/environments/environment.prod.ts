// Production environment configuration
// Update these values for production deployment

export const environment = {
  production: true,
  apiUrl: '/api/objects',
  apiKey: 'YOUR_PRODUCTION_API_KEY_HERE' // Set this in your CI/CD or deployment process
};